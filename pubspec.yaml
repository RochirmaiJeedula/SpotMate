name: spotmate
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0  # ✅ use this working version

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
